/*	This file is part of EasyFit, a nonlinear fitting program for the Macintoshª.		Copyright © 1989-1991 Matteo Vaccari & Mario Negri Institute.	All rights reserved.	file m_independ.h - definizioni che servono a permettere	di compilare senza troppi problemi sia sulla SUN 	che su Mac.		9/1/90	A causa di vari cambiamenti nel codice, questo include file non e' piu'	sufficiente per compilare senza problemi sulla SUN; lo lascio perche'	sara' sempre comodo averlo come base la prox volta che volessi compilare sulla	SUN.*/#ifndef __m_indep__#	define __m_indep__#	ifdef macintosh	#	include <string.h>#	include <strings.h>#	include <types.h>#	if TOOL#		include <stdio.h>#		define MESSAGESWRITELN(text)		fprintf(stderr, "%s\n", text)#		define MESSAGESWRITE(text)			fprintf(stderr, text)#		define MESSAGESNEWLINE()				fprintf(stderr, "\n")#		define POLLEVENT()							#		define NewPermPtr								NewPtr#		define FAILNIL(xxx)							if(!xxx) exit(999)				#	else not TOOL		pascal void MESSAGESWRITELN	(Str255 text) extern;		pascal void MESSAGESWRITE		(Str255 text) extern;		pascal void MESSAGESNEWLINE	(void) extern;		pascal void MESSAGESSYNCH		(void) extern;		pascal Boolean POLLEVENT		(void) extern;		#	endif not TOOL#	 define writeln(string)  MESSAGESWRITELN(string)#	 define write(string)    MESSAGESWRITE(string)#	 define newline          MESSAGESNEWLINE()#	 define synch		         MESSAGESSYNCH()		pascal void Failure(short error, long message);#	else	non-macintosh#		include <stdio.h>			typedef short         Boolean;		typedef double        extended;		typedef char					*Ptr;		/* Allocazione e deallocazione della memoria */#		define NewPtr(x)    malloc(x)#		define DisposPtr(x)   free(x)		/* Output del programma */#		define writeln(string)      printf("%s\n", (string))#		define write(string)        printf("%s", (string))#		define newline              printf("\n")#		define BIGBADERROR(x)				exit(x)#		define PROGRAMMINGERROR(s)	{ fprintf(stderr, (s)); exit(-999); }#	endif non-macintosh#endif