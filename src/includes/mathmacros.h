/*	Alcune macro		History: created Gioved“, 12 ottobre 1989 12:26:14*//* copia veloce di due array di extended */#define COPY_EVECTOR(dest,source,start,length) {extended *end,*d,*s; for(d=dest,s=source,end=s+(start)+(length); s<end; *d++ = *s++);}/* differenza tra due vettori di extended */#define EVECTOR_DIFFERENCE(dest,v1,v2,start,length) {short i, end; for(i=(start), end=(start)+(length); i<end; i++) (dest)[i] = (v1)[i]-(v2)[i];}/* inizializza una matrice quadrata a matrice identica:				{ short i,j;					for(i=(start);i<=(end);i++) {						for(j=(start);j<=(end);j++)							(mat)[i][j]=0.0;						(mat)[i][i]=1.0;					}				}*/#define IDENTITY(mat, start, end) { short i,j; for(i=(start);i<=(end);i++) {for(j=(start);j<=(end);j++) (mat)[i][j]=0.0; (mat)[i][i]=1.0;}}#define MAX(a,b) 				((a) > (b) ? (a) : (b))#define SHFT(a,b,c,d) 	{ (a)=(b); (b)=(c); (c)=(d); }